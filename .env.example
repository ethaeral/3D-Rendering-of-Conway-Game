# Mapbox GL JS access token (required for V3 Boston map).
# Get a free token at https://account.mapbox.com/
VITE_MAPBOX_ACCESS_TOKEN=pk.your_token_here

# Optional: override parcels source (dev uses mock API localhost:3001; prod uses ArcGIS API if unset).
# VITE_PARCELS_URL=
